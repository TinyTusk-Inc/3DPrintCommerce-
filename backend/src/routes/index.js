/**
 * Routes
 * 
 * Route files to be created:
 * - authRoutes.js (login, register, logout)
 * - productRoutes.js (list, detail, search, filter)
 * - orderRoutes.js (create, list, detail, cancel)
 * - reviewRoutes.js (create, update, delete, list)
 * - adminRoutes.js (products, orders, users, inventory management)
 * - customOrderRoutes.js (Phase 5: submit, list, detail, manage)
 */

// Example usage in index.js:
// const authRoutes = require('./authRoutes');
// const productRoutes = require('./productRoutes');
// app.use('/api/auth', authRoutes);
// app.use('/api/products', productRoutes);
